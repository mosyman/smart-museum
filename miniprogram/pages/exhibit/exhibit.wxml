<view class="container" wx:if="{{exhibit}}">
  <!-- 展品图片 -->
  <image class="exhibit-image" src="{{exhibit.imageUrl || '/images/default.jpg'}}" mode="aspectFill" />

  <!-- 展品信息 -->
  <view class="card">
    <view class="exhibit-header">
      <text class="exhibit-name">{{exhibit.name}}</text>
      <view class="exhibit-actions">
        <view class="action-icon {{isFavorited ? 'action-active' : ''}}" bindtap="toggleFavorite">
          <text>{{isFavorited ? '❤️' : '🤍'}}</text>
        </view>
        <view class="action-icon {{isPlaying ? 'action-playing' : ''}}" bindtap="playAudio">
          <text>{{isPlaying ? '⏸️' : '🔊'}}</text>
        </view>
      </view>
    </view>
    <text class="exhibit-category">{{exhibit.category}}</text>
    <text class="exhibit-description">{{exhibit.description}}</text>

    <!-- 位置信息 -->
    <view class="location-info">
      <text>📍 {{exhibit.locationArea}} {{exhibit.locationFloor}}楼</text>
    </view>

    <!-- 3D模型按钮 -->
    <button class="btn-3d" bindtap="viewModel3D" wx:if="{{exhibit.model3dUrl}}">
      🎨 查看3D模型
    </button>
  </view>
</view>

<!-- 友好的 404 提示 -->
<view class="not-found" wx:if="{{notFound}}">
  <text class="nf-emoji">🔍</text>
  <text class="nf-title">展品不存在</text>
  <text class="nf-desc">扫到的二维码可能不是本馆的，或该展品已下架。</text>
  <button class="nf-btn" bindtap="goBack">返回</button>
</view>